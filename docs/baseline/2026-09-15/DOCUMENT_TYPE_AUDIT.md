# Аудит типов документов

- Ветка: `site-v2`
- Дата проверки: 2026-09-23
- Исходный реестр: `docs/baseline/2026-09-15/DOCUMENT_MANIFEST.tsv`

## Результат полного прохода

Проверены по содержимому все 428 записей с документными расширениями (PDF, DOC, DOCX, XLSX) из манифеста. Для каждого файла проверены первые байты и, для OOXML, структура ZIP-пакета.

- PDF: 330 файлов с сигнатурой `%PDF-`.
- DOC: 22 файла с сигнатурой OLE Compound Document.
- XLSX: 2 файла с корректной OOXML-структурой.
- DOCX: 69 файлов с корректной OOXML-структурой.
- HTML-подмен и файлов с пустой/неизвестной сигнатурой не обнаружено.

## Расширенческие расхождения

Пять файлов имеют действительный бинарный формат, отличный от расширения:

- `src/assets/uploads/Профилактика-девиантного-поведения.docx` — legacy Word OLE (фактически DOC);
- `src/assets/uploads/Профилактика-эмоционального-выгорания.docx` — legacy Word OLE (фактически DOC);
- `src/assets/uploads/Развитие-нравственной-культуры-учащихся.docx` — legacy Word OLE (фактически DOC);
- `src/assets/uploads/памятка-для-педагогов-по-детскому-неблагополучию.docx` — legacy Word OLE (фактически DOC);
- `src/assets/uploads/метод-реком-для-педагогов.docx` — PDF 1.7 (сигнатура `%PDF-`).

Файлы не переименовывались: изменение URL без отдельного решения может сломать существующие ссылки. Фактический тип каждого расхождения зафиксирован для следующего этапа проверки актуальности и дублей.

## Отдельная проверка файла из чеклиста

`src/assets/uploads/mto/eor/38.02.01_perechen-EOR.pdf` проверен отдельно: сигнатура `%PDF-1.5`, файл распознаётся как PDF на 2 страницы. Это не HTML-файл и не требует исправления или удаления по прежнему основанию.

## Метод

1. Получен список 428 документов из `DOCUMENT_MANIFEST.tsv`.
2. Прочитаны первые байты каждого файла в локальном checkout ветки `site-v2`.
3. PDF проверены по сигнатуре `%PDF-`; старые форматы DOC — по OLE-сигнатуре; DOCX/XLSX — по ZIP-сигнатуре и наличию `[Content_Types].xml`.
4. Для отдельного файла `38.02.01_perechen-EOR.pdf` дополнительно проверена структура PDF и число страниц через `pdfinfo`.

## Статус

**PASS для пункта 2.5.1:** полный побайтный проход выполнен. Расширенческие расхождения не скрыты и переданы в следующий пункт проверки.
