# Проверка ссылок на документы и host-only assets после генерации

- Ветка: `site-v2`
- Дата проверки: 2026-09-23
- Checkout: чистый shallow checkout ветки `site-v2`
- Команды: `npm ci`, `npm run build`, локальный Eleventy server, `node scripts/check-links.mjs` по 144 URL из `URL_MANIFEST.tsv`

## Результат сборки и ссылок

- `npm ci`: PASS.
- `npm run build`: PASS; Eleventy записал 147 HTML-страниц и скопировал 647 ассетов.
- Контрольные маршруты: 144 из 144 ответили HTTP 200.
- Страницы без ошибок ссылок: 139.
- Страницы с ошибками: 5.
- Уникальные URL ресурсов, отсутствующие в чистом V2-output: 12.
- Из этих 12 URL на опубликованном сайте 11 отвечают HTTP 200, потому что были загружены напрямую на хостинг; один URL уже отвечает HTTP 404.

## Host-only resources

Следующие ресурсы отсутствуют в checkout/repository output, но доступны на опубликованном хостинге и не должны считаться автоматически удалёнными:

- `/docs/kontrakt/Служба-по-контракту-ролик.mp4` — HTTP 200.
- `/docs/kontrakt/3.1.mp4` — HTTP 200.
- `/docs/kontrakt/4.1.mp4` — HTTP 200.
- `/docs/kontrakt/5.1.mp4` — HTTP 200.
- `/docs/kontrakt/6.1.mp4` — HTTP 200.
- `/assets/uploads/i.jpeg` — HTTP 200.
- `/docs/obrazovanie/Screenshot-2025-07-17-at-07-42-57-Отчет-о-проверке-ЭП.png` — HTTP 200.
- `/docs/obrazovanie/вакантные-места-01.04.2026_page-0001.jpg` — HTTP 200.
- `/docs/obrazovanie/вакантные-места-01.04.2026_page-0002.jpg` — HTTP 200.
- `/docs/obrazovanie/вакантные-места-01.04.2026_page-0003.jpg` — HTTP 200.
- `/docs/obrazovanie/вакантные-места-01.04.2026_page-0004.jpg` — HTTP 200.

Текущий `deploy.sh` очищает `public_html` полностью, кроме `docs/`, `submit-form.php`, `forms.log` и `.htaccess`. Поэтому host-only ресурсы под `/docs/` сохраняются, а `/assets/uploads/i.jpeg` будет удалён при деплое, если его не добавить в репозиторий или отдельно не изменить правила очистки.

## Реальные проблемы

### 1. Битая ссылка на видео

Решение владельца от 23.09.2026: выбран вариант A — битая ссылка на `/assets/uploads/employment/marshrutizatsiya-molodezhi-mintrud-rf.mp4` удалена из `src/content/pages/svedenija/employment/index.md`. Исходный MP4 отсутствует и в checkout, и на опубликованном сайте; точная официальная замена не подтверждена.

Варианты исправления:
- **Выбрано:** удалить ссылку, так как локальный ролик недоступен;
- заменить на точную официальную ссылку на ролик Минтруда, если такой URL будет подтверждён;
- добавить исходный MP4 в репозиторий и публиковать его из `src/assets/uploads/employment/` после проверки прав.

### 2. Изображение `i.jpeg`

`src/content/pages/professionaly-2026/index.md` ссылается на `/assets/uploads/i.jpeg`, но файл отсутствует в репозитории и намеренно исключён из passthrough в `.eleventy.js`. Сейчас на хостинге он отвечает HTTP 200, но после текущей очистки deploy-скрипта эта ссылка станет битой.

Варианты исправления:
- добавить оригинал в репозиторий после проверки происхождения и прав;
- убрать изображение/ссылку из страницы, если материал нельзя подтвердить;
- отдельно менять deploy-скрипт для сохранения `assets/uploads` — менее предпочтительно, поскольку это сохраняет невоспроизводимое состояние хостинга.

## Статус пункта чек-листа

Пункт выполнен частично: ссылка-проверка воспроизведена после чистой сборки, host-only ресурсы отделены от реальных ошибок, найдено одно уже битое видео и один риск удаления изображения при деплое. Для закрытия пункта нужно принять решения по этим двум ссылкам и повторить проверку.