# Проверка текущего домена по HTTP/HTTPS

Дата проверки: 2026-09-15 08:22 UTC  
Источник адреса: `src/_data/site.yaml` и `docs/DEPLOY.md`  
Домен: `сит-сальск.рф`  
Punycode: `xn----8sbwke6acce8h.xn--p1ai`

Проверка выполнена внешним HTTP-клиентом, не через локальный Eleventy preview.

## Результаты

| Запрос | Результат |
|---|---|
| `http://сит-сальск.рф/` без перехода | HTTP 301 Moved Permanently |
| HTTP-заголовок редиректа | `Location: https://сит-сальск.рф/` |
| HTTP с переходом | HTTP 200, один редирект, итоговый URL HTTPS |
| `https://сит-сальск.рф/` | HTTP/2 200 |
| HTTPS Content-Type | `text/html` |
| HTTPS TLS verification | `ssl_verify_result=0` — ошибок проверки сертификата не обнаружено |
| HTTPS response size | 83 105 bytes |
| Внешнее содержимое страницы | Заголовок «Главная страница | ГБПОУ РО \"Сальский индустриальный техникум\"» |

## Вывод

HTTP корректно переводит посетителя на HTTPS. HTTPS доступен напрямую, отдаёт главную страницу с кодом 200 и валидным TLS-сеансом. Проверка выполнена для корневого URL текущего домена; отдельная проверка маршрутов и документов относится к другим пунктам чек-листа.

Связанный пункт: раздел 1, проверка текущего домена по HTTP/HTTPS из внешней среды.
