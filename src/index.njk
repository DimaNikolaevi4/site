---
layout: layouts/base.njk
title: Главная страница
description: Официальный сайт Сальского индустриального техникума
---

<main class="main">

  <!-- Hero Section -->
  <section id="hero" class="hero section dark-background">

    <img src="/assets/mentor/img/hero-bg.jpg" alt="" data-aos="fade-in">

    <div class="container">
      <div class="site-title" data-aos="fade-up" data-aos-delay="100">
        ГБПОУ РО<br>
        <small>«Сальский индустриальный техникум»</small>
      </div>
    </div>

  </section><!-- /Hero Section -->

  <!-- About Section (Секция 4) -->
  {% set aboutMode = 'director' %}
  {% include "components/about.njk" %}

  <!-- Секции 5 + 7: Новости (70%) + Сайдбар с баннерами (30% слева) -->
  <section id="news" class="courses section">

    <div class="container section-title" data-aos="fade-up">
      <h2>Новости</h2>
      <p>Последние события техникума</p>
    </div>

    <div class="container">
      <div class="content-sidebar-grid">

        <!-- Секция 7: Сайдбар (30% слева, единые баннеры) -->
        <div class="sidebar-col">
          {% include "components/sidebar.njk" %}
        </div>

        <!-- Лента новостей (70% справа) -->
        <div class="content-col">
          <div class="row">
            {% for post in collections.news | reverse %}
            {% if loop.index <= 3 %}
            <div class="col-md-6 col-xl-4 d-flex align-items-stretch" data-aos="zoom-in" data-aos-delay="{{ loop.index * 100 }}">
              <div class="course-item">
                <img src="{{ post.data.image or '/assets/mentor/img/course-' ~ (loop.index) ~ '.jpg' }}" class="img-fluid" alt="{{ post.data.title }}">
                <div class="course-content">
                  <div class="d-flex justify-content-between align-items-center mb-3">
                    <p class="category">{{ post.data.category or 'Новости' }}</p>
                    <p class="date">{{ post.date | dateRu }}</p>
                  </div>

                  <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                  <p class="description">{{ post.data.excerpt or post.templateContent | truncate(120) }}</p>
                  <div class="trainer d-flex justify-content-between align-items-center">
                    <div class="trainer-profile d-flex align-items-center">
                      <a href="{{ post.url }}" class="trainer-link">Читать далее →</a>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            {% endif %}
            {% else %}
            <div class="col-12 text-center">
              <p>Новости пока не добавлены.</p>
            </div>
            {% endfor %}
          </div>

          <div class="text-center mt-4" data-aos="fade-up" data-aos-delay="500">
            <a href="/news/" class="btn-get-started">Все новости</a>
          </div>
        </div>

      </div>
    </div>

  </section><!-- /News + Sidebar -->

  <!-- Секция 6: Популярные разделы (единый компонент) -->
  {% include "components/popular.njk" %}

</main>
