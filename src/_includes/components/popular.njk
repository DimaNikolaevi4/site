{# ════════════════════════════════════════════════════════════════════
   КОМПОНЕНТ: Секция 6 — «Популярное» (канон 3.0.1)

   Блок популярных/важных материалов — 3 карточки в Bootstrap-сетке
   `col-md-6 col-xl-4` + `course-item` (тот же визуальный язык, что
   у news.njk в режиме home).

   Канонический файл компонента — этот. Содержимое (логика 3 карточек
   + автозамена «карточки самой на себя» на placeholder) исторически
   было реализовано в `components/important-info.njk` и используется
   на всех страницах сайта. Чтобы привести имя файла в соответствие
   с каноном 3.0.1 и при этом не дублировать разметку, этот файл —
   тонкий алиас, который подключает существующую реализацию.

   Используется в шаблонах: index.njk, page-full.njk, page.njk,
   post.njk, listing.njk.
   ════════════════════════════════════════════════════════════════════ #}
{% include "components/important-info.njk" %}
